# Copy to .env.local and adjust values for your deployment.
# All NEXT_PUBLIC_ vars are inlined at build time — do NOT put secrets here.

# WebSocket backend URL (defaults to ws://localhost:8000/ws if unset)
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws
